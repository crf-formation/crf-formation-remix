//
// /!\ Those constants are shared with the browser
//
export type ThemeNames = "dark" | "light";

export const DEFAULT_THEME: ThemeNames = "light";

// drawer (left sidebar)
export const DRAWER_WIDTH = "256px";
export const CLOSED_DRAWER_WIDTH = "0px";

export const USER_PASSWORD_MIN_LENGTH = 8;

export const DEFAULT_LOCALE = "en-US";